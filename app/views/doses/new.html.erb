<h1>New Dose</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredients, collection: 0..5 %>
  <%= f.submit "Add dose", class: "btn btn-primary" %>
<% end %>
